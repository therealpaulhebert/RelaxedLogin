{{>header}}

<h1>{{h1}}</h1>
<p class="lead">Add a new team and it's initial member here, you can add more members after creating the team. You can also click on any team below to edit the team's details, including its members.</p>
<div class="mt-2">
  <div class="row">
    <div class="col-8">
      <h4>Add a New Team</h4>
      <form action="/teams" method="post" class="mb-3">
        <div class="row">
          <div class="col-sm-6">
            <div class="form-group">
              <label for="name">Team Name</label>
              <input type="text" id="name" name="name" class="form-control" required></input>
            </div>
          </div>
          <div class="col-sm-6">
            <div class="form-group">
              <label for="description">Description</label>
              <input type="text" id="description" name="description" class="form-control"></input>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col">
            <label for="members.names">Initial Team Member</label>
          </div>
          <div class="col">
            <label for="members.roles">Initial Role</label>
          </div>
        </div>
        <div class="row">
          <div class="col">
            <div class="form-group">
              <select id="members.name" name="members.name" class="form-control" placeholder="New Member Name">
                {{#users}}
                  <option>{{name}}</option>
                {{/users}}
              </select>

            </div>
          </div>
          <div class="col">
            <div class="form-group">
              <input type="text" id="members.role" name="members.role" class="form-control" placeholder="New Member Role"></input>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col">
            {{#users}}
              <!--
      <div class="form-check">
        <input  class="form-check-input" type="checkbox" name="member.name" id="member.name" value="{{name}}">
        <label class="form-check-label" for="member.name">{{name}}</label>
    </div>
  -->
            {{/users}}
          </div>
        </div>

        <div class="row">
          <div class="col">
            <input type="hidden" name="action" value="add" />
            <input type="hidden" name="type" value="team" />
            <input name="_csrf" type="hidden" value="{{_csrf.token}}" />
            <button class="btn btn-primary ml-auto" type="submit">Create Team</button>
          </div>
        </div>

      </form>
    </div>
    <div class="col">
      <img src="/images/pitbullSm.png" class="img-fluid" />
    </div>
  </div>

  <div class="row">
    <div class="col">
      <h4>Edit Existing Teams</h4>

<table class="table table-striped">
<thead>
  <tr>
    <th scope="col">Team</th>
    <th scope="col">Description</th>
    <th scope="col"><img src="/images/pencilSm.png" alt="Edit Column" class="img-fluid"/></th>
  </tr>
</thead>
<tbody>
        {{#teams}}
        <tr>
        <td>{{name}}</td> <td>{{description}}</td> <td><a href="/team/{{id}}">Edit</a></td>
      </tr>
      <!--
          <a href="/editTeam/{{id}}" class="list-group-item list-group-item-action"><strong>{{name}}</strong> <small>{{description}}</small></a>
        -->
        {{/teams}}
</tbody>
</table>
    </div>
  </div>
</div>
{{>footer}}
